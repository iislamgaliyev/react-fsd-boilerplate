export enum AppPages {
	RootPage = "/",
	NotFoundPage = "/404",
	AnyPage = "*",
}

export enum AuthPages {
	Root = "/auth",
	SignIn = "/auth/sign-in",
	SignUp = "/auth/sign-up",
	// *Specify other auth related route values...
}

export enum DashboardPage {
	Root = "/dashboard/main",
	// *Specify other dashboard related route values...
}
